class Player extends GameObject
{
  Player(PVector position, float w, float h, color c)
  {
    super(position, w, h, c);
  }
  
  void update()
  {
    
  }
  
  void render()
  {
    //fill(c);
    //ellipse(position.x, position.y, w, h);
  }
}